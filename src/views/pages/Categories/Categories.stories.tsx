import React from 'react';
import Categories from './';

export default {
  title: 'Views/Pages/Categories',
  component: Categories,
};

export const StoryCategories = (args) => <Categories {...args} />;
