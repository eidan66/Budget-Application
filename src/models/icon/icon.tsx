import * as E from '../../containers/enums/icon';

export interface Icon {
  icon: E.icon;
  color?: string;
  fontSize?: E.fontSize;
}

export interface Paypal {
  color?: string;
  fontSize?: string;
}
